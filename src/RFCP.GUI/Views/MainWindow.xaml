<Window x:Class="RFCP.GUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="RFCP - Robot Flexible Control Platform"
        Height="420"
        Width="760">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="22"
                   FontWeight="SemiBold"
                   Text="RFCP Operator Console" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,16,0,16">
            <Button Width="180"
                    Height="34"
                    Content="Start Sample Task"
                    Command="{Binding StartSampleTaskCommand}" />
            <TextBlock Margin="16,8,0,0"
                       Text="{Binding Status}" />
        </StackPanel>

        <Border Grid.Row="2" BorderThickness="1" BorderBrush="#DDDDDD" CornerRadius="4" Padding="12">
            <TextBlock TextWrapping="Wrap"
                       Text="TODO: Add production dashboard controls, alarms, and recipe/task panels." />
        </Border>
    </Grid>
</Window>
